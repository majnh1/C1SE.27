import 'package:flutter/material.dart';

class FoodieModel extends StatelessWidget {
  const FoodieModel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}